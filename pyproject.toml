[project]
name = "dotoro-backend"
version = "0.1.0"
description = "Backend for Dotoro Prototype"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0",
    "websockets>=12.0",
]
